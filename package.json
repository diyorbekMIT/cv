{
  "name": "cv-website",
  "version": "1.0.0",
  "private": true,
  "description": "Personal CV + Portfolio + Blog website — Heroku build entry point",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node server/index.js",
    "heroku-postbuild": "npm ci --prefix server --omit=dev && npm ci --prefix client && npm run build --prefix client && rm -rf client/node_modules"
  }
}
